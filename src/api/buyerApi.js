// Buyer API endpoints
export const getAvailableProduce = async () => {
  return [];
};

export const makePurchaseRequest = async (produceId, quantity, price) => {
  return { success: true };
};

export const getMyRequests = async (buyerId) => {
  return [];
};
